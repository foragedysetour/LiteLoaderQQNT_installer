name: Build LiteLoaderQQNT_installer

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: 下载源码
      uses: actions/checkout@v6
      
    - name: 安装Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12.10'
        
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install plyer
        pip install maliang
        pip install pyinstaller
        pip install requests
        
    - name: 使用PyInstaller构建可执行文件（包含资源）
      run: |
        pyinstaller -w -F -i logo.ico --hidden-import plyer.platforms.win.notification LiteLoaderQQNT_installer.py
    - name: 将资源文件复制到dist目录
      run: |
        copy .\uninstall.bat dist\
    - name: 上传构建工件
      uses: actions/upload-artifact@v6
      with:
        name: LiteLoaderQQNT_installer-executable
        path: dist/
        retention-days: 30
